import 'package:flutter/material.dart';

class Translate {
  static final supportedLanguage = [
    const Locale('en', 'US'),
    const Locale('km', 'KH'),
  ];

  static const defaultLanguage = Locale('en', 'US');
  static const englishLanguage = Locale('km', 'KH');
}
